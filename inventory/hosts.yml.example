---
# Copier en inventory/hosts.yml (gitignoré : il porte adresses et accès SSH).
all:
  children:
    ciso:
      hosts:
        bdfg:                                   # → host_vars/bdfg.yml
          ansible_host: BDFG-SRV-SEC01
          ansible_user: admrl
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519
